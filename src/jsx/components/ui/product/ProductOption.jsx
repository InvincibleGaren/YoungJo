import React from 'react'

function ProductOption({option}) {

    // console.log(option);
    // console.log(option[0].optValue)

    return (
        {
            // option && option.map((item) => (
            //     <option value="">{item.optValue}</option>
            // ))
            // <option value="">{optValue}</option>
        }
    );
}

export default ProductOption